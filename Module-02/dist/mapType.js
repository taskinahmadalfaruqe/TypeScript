"use strict";
{
    // Blocked Scope
    const arrNumber = [1, 2, 3, 4, 3];
    const arrString = ["1", "2", "3", "4"];
    const arrString02 = arrNumber.map((number) => number.toString());
    console.log(arrString02);
    const AreaCalculate = {
        height: 100,
        width: 20,
    };
    // Blocked Scope
}
