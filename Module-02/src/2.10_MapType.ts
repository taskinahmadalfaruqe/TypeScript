{
    // Block Scoping

    // Map Type
    const numberOfArray: number[] = [1, 2, 3, 4, 5];
    const stringArray: string[] = numberOfArray.map(String);
    console.log(stringArray)
    // Block Scoping
}