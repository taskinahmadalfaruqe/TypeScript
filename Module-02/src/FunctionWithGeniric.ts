{
  // Block Scoped
  // Function With Generic
  const creatArray = (parems: string): string[] => {
    return [parems];
  };
  console.log(creatArray("Bangladesh"));

  // Create Array with Generic 
  // Block Scoped
}
